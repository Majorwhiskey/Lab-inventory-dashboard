PORT=4000
CLIENT_URL=http://localhost:4000

# Optional: absolute path to inventory Excel (default: Varaha_Inventory_Update.xlsx then legacy March file in project root)
# INVENTORY_EXCEL_PATH=/path/to/Varaha_Inventory_Update.xlsx

# One-time full re-import from Excel (clears items table). Set to 1, restart server, then remove.
# REIMPORT_INVENTORY=1
